<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
            <item
                android:id="@+id/nav_home"
                android:title="Inicio" />
    </group>

    <item android:title="Contacto">
        <menu>
            <item
                android:id="@+id/nav_contact"
                android:icon="@android:drawable/ic_menu_call"
                android:title="@string/menu_contact" />
            <item
                android:id="@+id/nav_messenger"
                android:icon="@drawable/messenger_icon"
                app:showAsAction="always"
                android:title="@string/menu_messenger" />
            <item
                android:id="@+id/nav_whatsapp"
                android:icon="@drawable/whatsapp_icon"
                android:title="@string/menu_whastapp" />
            <item
                android:id="@+id/nav_chat"
                android:icon="@drawable/chat_icon"
                android:title="Chat" />
        </menu>
    </item>

</menu>
